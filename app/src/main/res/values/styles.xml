<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="FullScreen" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="label_app">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:text">@string/titulo</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">@dimen/tamanho_titulo</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/tituloApp</item>
    </style>

    <style name="label_footer" parent="label_app">
        <item name="android:text">@string/footer</item>
        <item name="android:textSize">@dimen/tamanho_footer</item>
        <item name="android:textStyle">italic</item>
    </style>

    <style name="Fiap">
        <item name="android:textColor">@color/colorText</item>
    </style >

    <style name="Fiap.Header">
        <item name="android:background">@color/colorPrimaryDark</item>
        <item name="android:padding">@dimen/padding_padrao</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="android:layout_marginBottom">@dimen/padding_padrao</item>
        <item name="android:layout_marginTop">@dimen/padding_padrao</item>
    </style>


    <style name="Fiap.Botao">
        <item name="android:background">@drawable/botao_personalizado</item >
        <item name="android:textSize">@dimen/botao_tamanho_texto</item >
    </style>


    <string name="titulo">Pizza</string>
    <string name="footer">Desenvolvido por: Paty</string>
</resources>
